# คู่มือสำหรับผู้ดูแลระบบสูงสุด (Super Admin)

คู่มือนี้สำหรับบทบาท `super_admin` ที่มีสิทธิ์สูงสุดในระบบ สามารถจัดการผู้ใช้ ทีม และตั้งค่าระบบได้

---

## 1. เริ่มต้นใช้งาน

### 1.1 ล็อกอิน

1. เปิดเว็บเบราว์เซอร์
2. เข้า URL ของระบบ
3. กรอก `username` และ `password`
4. กดปุ่ม `เข้าสู่ระบบ`

> หากระบบแจ้งว่า "กรุณาเข้าสู่ระบบใหม่" ให้ล็อกอินใหม่อีกครั้ง

### 1.2 เมนูที่ super_admin จะเห็น

- หน้าแรก
- ระบบเช็คอิน
- น้ำมันและยานพาหนะ
- ระบบจัดส่งอัจฉริยะ
- ระบบคลังสินค้า
- ตั้งค่าระบบเว็บไซต์
- จัดการผู้ใช้

> เมนู `ตั้งค่าระบบเว็บไซต์` สำหรับ `super_admin` เปิดหน้าตั้งค่าประกาศหน้าเว็บ ซึ่งสามารถกรอกข้อความหรือแนบรูปประกาศได้ เมื่อบันทึกแล้วผู้ใช้ทุกคนจะเห็นหน้าต่างประกาศป๊อปอัปพร้อมปิดได้

---

## 2. การจัดการผู้ใช้

### 2.1 ดูรายชื่อผู้ใช้

1. เลือกเมนู `จัดการผู้ใช้`
2. ตรวจสอบรายชื่อผู้ใช้งานทั้งหมด
3. ดูบทบาทและสถานะผู้ใช้

### 2.2 เพิ่มผู้ใช้ใหม่

1. กด `เพิ่มผู้ใช้`
2. กรอกข้อมูลผู้ใช้ใหม่
3. ระบุบทบาท เช่น `admin`, `technician`, `sales`, `user`
4. กดบันทึก

### 2.3 อนุมัติผู้ใช้

1. ไปที่ `ผู้ใช้รอดำเนินการ`
2. ตรวจสอบข้อมูลผู้สมัคร
3. กด `อนุมัติ` หรือ `ปฏิเสธ`

### 2.4 ลบหรือแก้ไขผู้ใช้

1. เลือกผู้ใช้จากรายการ
2. กดแก้ไขเพื่อปรับบทบาทหรือข้อมูล
3. หากต้องการลบ ให้กดปุ่ม `ลบ`

---

## 3. ระบบจัดส่งอัจฉริยะ

### 3.1 ดูและบริหารงาน

- เข้าระบบ `Dispatch`
- ตรวจสอบสถานะงาน
- เพิ่ม/แก้ไขทีม
- จัดการการจ่ายงานอัตโนมัติ

### 3.2 การจัดการทีม

1. เปิดเมนูทีม
2. สร้างทีมใหม่
3. ลบหรือแก้ไขทีมเดิม

### 3.3 ดูสถิติการทำงาน

- ดูจำนวนงานทั้งหมด
- ตรวจสอบงานมีพิกัด งานรอจ่าย งานสำเร็จ
- ใช้ข้อมูลนี้วางแผนการจัดทีม

---

## 4. ระบบน้ำมันและยานพาหนะ

### 4.1 ตรวจสอบรายงานน้ำมัน

- ดูสถิติ น้ำมัน รายงาน km/l
- ตรวจสอบยอดรวมและค่าบริหาร
- ใช้ฟิลเตอร์วันที่เพื่อจำกัดข้อมูล

### 4.2 นำเข้าและส่งออกข้อมูล

- นำเข้าไฟล์ Excel
- ส่งออกข้อมูลน้ำมันตามทีม/รถและเดือน

---

## 5. ระบบคลังสินค้า

### 5.1 ตรวจสอบสต็อก

- ดูรายการสินค้า
- ตรวจสอบปริมาณคงคลัง
- ตรวจสอบประวัติการโอน

### 5.2 จัดการสินค้าและรุ่น

- สร้างสินค้าใหม่
- แก้ไขข้อมูลสินค้า
- ลบสินค้าเก่า

---

## 6. ปัญหาที่มักเจอสำหรับ super_admin

### Q1: ทำไมไม่เห็นเมนู `จัดการผู้ใช้`?
A: ตรวจสอบว่าบัญชีคุณเป็น `super_admin` จริง หากไม่ใช่ระบบจะซ่อนเมนูนี้

### Q2: ทำไมไม่สามารถสร้างทีมได้?
A: ตรวจสอบสิทธิ์และข้อมูลทีมที่กรอกให้ครบถ้วน

### Q3: ทำไมผู้ใช้ไม่สามารถล็อกอินได้?
A: ตรวจสอบบัญชีผู้ใช้ว่าถูกปิดใช้งานหรือยังไม่ได้อนุมัติ

---

## 7. สรุป

1. เข้าสู่ระบบด้วยบัญชี `super_admin`
2. จัดการผู้ใช้และสิทธิ์การเข้าถึง
3. ตรวจสอบ Dispatch, น้ำมัน และคลังสินค้า
4. อนุมัติหรือปฏิเสธผู้ใช้ใหม่

**END**