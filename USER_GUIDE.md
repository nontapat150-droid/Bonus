# คู่มือทั่วไปสำหรับผู้ใช้งาน

คู่มือนี้สั้นและตรงไปตรงมาสำหรับผู้ใช้ทั่วไป (sales / user)

---

## 1. เข้าสู่ระบบ

1. เปิดเว็บเบราว์เซอร์
2. เข้า URL ของระบบ
3. กรอกชื่อผู้ใช้และรหัสผ่าน
4. กดปุ่ม `เข้าสู่ระบบ`

> หากระบบแจ้งว่า "กรุณาเข้าสู่ระบบใหม่" ให้ล็อกอินใหม่อีกครั้ง

---

## 2. เมนูหลัก

- หน้าแรก: สรุปข้อมูลทั่วไป
- ระบบเช็คอิน: บันทึกการเข้าออกงาน
- น้ำมันและยานพาหนะ: ตรวจสอบการเติมน้ำมัน
- ระบบจัดส่งอัจฉริยะ: ดูงานจัดส่ง (สำหรับช่าง/ผู้ดูแล)
- ระบบคลังสินค้า: สำหรับ admin / super_admin
- จัดการผู้ใช้: สำหรับ super_admin

> หากไม่เห็นเมนู แปลว่าไม่ได้รับสิทธิ์สำหรับเมนูนั้น

---

## 3. การใช้งานที่ควรรู้

- หากเป็น `sales` จะเห็นเฉพาะหน้าระบบเช็คอินและหน้าแรก
- หากเป็น `technician` จะเห็นเช็คอิน น้ำมัน และ Dispatch
- หากเป็น `admin` จะเห็นหน้าทั้งหมด ยกเว้นจัดการผู้ใช้
- หากเป็น `super_admin` จะเห็นทุกเมนู รวมทั้งตั้งค่าระบบเว็บไซต์

---

## 4. ออกจากระบบ

1. กดปุ่ม `ออกจากระบบ`
2. รอระบบยืนยัน
3. เข้าสู่ระบบใหม่เมื่อจำเป็น

---

## 5. ถ้ามีปัญหา

- ลองรีเฟรชหน้า
- ล็อกอินใหม่
- หากยังไม่หาย ให้บันทึกหน้าจอและติดต่อผู้ดูแล

---

## 6. ข้อแนะนำ

- เริ่มจากหน้าแรกทุกครั้ง
- ใช้ฟิลเตอร์วันที่เมื่อดูข้อมูลน้ำมัน
- หากไม่แน่ใจ ให้สอบถามผู้จัดการทีมก่อนบันทึกข้อมูล

**END**
