# คู่มือการใช้งานระบบ Bonus

คู่มือนี้ถูกออกแบบสำหรับมือใหม่ที่ใช้งานระบบครั้งแรก อ่านง่าย และสรุปขั้นตอนปฏิบัติจริงให้ชัดเจนที่สุด

---

## 1. ภาพรวมระบบ
ระบบนี้เป็นระบบจัดการงานภาคสนามและระบบน้ำมันที่รวมฟีเจอร์หลักไว้ดังนี้:

- ระบบเช็คอินของช่าง
- ระบบน้ำมันและยานพาหนะ
- ระบบจัดส่งอัจฉริยะ (Dispatch)
- ระบบคลังสินค้าสำหรับผู้ดูแล
- การนำเข้าและส่งออกข้อมูล Excel

**บทบาทผู้ใช้หลัก:**
- `super_admin` — ผู้ดูแลระบบสูงสุด
- `admin` — ผู้ดูแลระบบ
- `technician` — ช่างเทคนิค
- `sales` — เซล (เข้าใช้ได้เฉพาะบางหน้า)
- `user` — ผู้ใช้งานทั่วไป

---

## 1.1 คู่มือแยกตามบทบาท

เพื่อความรวดเร็วและไม่สับสน ผู้ใช้แต่ละบทบาทควรเปิดคู่มือเฉพาะบทบาทของตนเองเท่านั้น:

- `USER_GUIDE_TECHNICIAN.md` — คู่มือสำหรับช่างเทคนิค
- `USER_GUIDE_ADMIN.md` — คู่มือสำหรับผู้ดูแลระบบ (admin)
- `USER_GUIDE_SUPER_ADMIN.md` — คู่มือสำหรับผู้ดูแลระบบสูงสุด (super_admin)

> หากคุณเป็นช่าง ให้เปิด `USER_GUIDE_TECHNICIAN.md`
> หากคุณเป็น admin ให้เปิด `USER_GUIDE_ADMIN.md`
> หากคุณเป็น super_admin ให้เปิด `USER_GUIDE_SUPER_ADMIN.md`

---

## 2. เริ่มต้นใช้งาน

### 2.1 ล็อกอิน

1. เปิดเว็บเบราว์เซอร์
2. เข้า URL ของระบบ
3. กรอก `username` และ `password`
4. กดปุ่ม `เข้าสู่ระบบ`

> หากมีการแจ้งว่า "กรุณาเข้าสู่ระบบใหม่" ให้ล็อกอินใหม่อีกครั้ง เพราะ session หมดอายุหรือข้อมูลล็อกอินไม่ถูกต้อง

### 2.2 เมนูหลัก

ฝั่งซ้ายของหน้าจอจะมีเมนูหลักดังนี้:

- หน้าแรก
- ระบบเช็คอิน
- น้ำมันและยานพาหนะ
- ระบบจัดส่งอัจฉริยะ
- ระบบคลังสินค้า (เฉพาะ admin / super_admin)
- จัดการผู้ใช้ (เฉพาะ super_admin)

หากไม่เห็นเมนูบางรายการ แปลว่าไม่มีสิทธิ์ใช้งานสำหรับบทบาทนั้น

---

## 3. ระบบน้ำมันและยานพาหนะ

### 3.1 ฟังก์ชันหลัก

ในหน้า `น้ำมันและยานพาหนะ` คุณจะสามารถ:

- ดูสถิติรวมรายวันและรายเดือน
- ดูสรุปยอดเงิน น้ำมัน ระยะทาง และค่าเฉลี่ย กม./ลิตร
- กรองข้อมูลตามวันที่
- เลือกดูเฉพาะรถหรือทีมที่ต้องการ
- เพิ่มบันทึกเติมน้ำมันย้อนหลัง
- นำเข้า Excel ข้อมูลบันทึกน้ำมัน
- ส่งออก Excel ข้อมูลสำคัญ
- คำนวณเลขไมล์ใหม่หากมีการแก้ไขข้อมูล

### 3.2 วิธีใช้งานหน้า Oil Report

#### กรองช่วงเวลา

1. เลือก `วันที่เริ่มต้น` และ `วันที่สิ้นสุด`
2. กดปุ่ม `ตกลง`
3. ระบบจะแสดงข้อมูลที่ตรงกับช่วงเวลานั้น

#### เลือกทีม/รถ

1. เลือกจากเมนูดรอปดาวน์ด้านบน
2. ระบบจะกรองเฉพาะรถหรือทีมที่เลือก

#### ดูข้อมูลตาราง

ตารางจะแสดงรายละเอียดของแต่ละรอบการเติมน้ำมัน เช่น:

- วันที่/เวลา
- ช่างผู้เติม
- ทีม/ทะเบียนรถ
- เลขไมล์
- ระยะทางวิ่ง
- กม./ลิตร
- จำนวนงาน
- ต้นทุนต่อกม.
- ยอดเติม

### 3.3 เพิ่มข้อมูลน้ำมันย้อนหลัง

1. กดปุ่ม `เพิ่มข้อมูล`
2. กรอกข้อมูลในหน้าฟอร์ม:
   - วันที่บันทึก
   - ช่างผู้เติม
   - ทะเบียนรถ/ทีม
   - ลิตร
   - ราคาต่อลิตร
   - ยอดเงิน
   - เลขไมล์
3. กด `บันทึก`

> ช่อง `ยอดเงิน` จะคำนวณอัตโนมัติเมื่ากรอก `ลิตร` และ `ราคาต่อลิตร`

### 3.4 นำเข้า Excel

1. กดปุ่ม `นำเข้า`
2. เลือกไฟล์ `.xlsx` หรือ `.xls`
3. ระบบจะอ่านข้อมูลและนำเข้ารายการ

**คำแนะนำ**
- ตรวจสอบว่าไฟล์มีหัวคอลัมน์ที่ถูกต้อง
- ไม่ควรนำเข้าไฟล์ซ้ำโดยไม่ตรวจสอบก่อน

### 3.5 ส่งออก Excel

1. กดปุ่ม `ส่งออก`
2. เลือกรถ/ทีมที่ต้องการ
3. เลือกเดือน
4. ระบบจะดาวน์โหลดไฟล์ Excel

ไฟล์ Excel จะมีคอลัมน์หลัก:

- วันที่
- ชื่อผู้เติม
- เลขไมล์
- ระยะทางวิ่ง (กม.)
- จำนวนลิตร
- ราคาต่อลิตร
- กม./ลิตร
- ยอดเติม (บาท)

---

## 4. ระบบจัดส่งอัจฉริยะ (Dispatch)

### 4.1 ภาพรวมหน้า Dispatch

หน้าจอระบบจัดส่งจะแบ่งเป็น:

- แท็บรายการงาน
- แท็บแผนที่
- สถิติงาน เช่น จำนวนงานทั้งหมด งานที่มีพิกัด งานจ่ายแล้ว งานรอจ่าย
- ปุ่มสร้างทีม / ปุ่มจ่ายงาน / ปุ่มลบงาน

### 4.2 การดูรายการงาน

ในแท็บ `รายการงาน` คุณจะเห็นงานทั้งหมด พร้อมข้อมูลสำคัญ เช่น:

- เลขที่สั่งงาน
- ชื่อลูกค้า
- พิกัด
- ทีมที่รับงาน
- สถานะงาน
- ปุ่มดูรายละเอียด

### 4.3 การดูแผนที่

ในแท็บ `แผนที่` จะแสดงงานที่มีพิกัดบนแผนที่

- งานที่มีพิกัด: จะมีหมุดสี
- งานที่ไม่มีพิกัด: จะแสดงในรายการ แต่ไม่ขึ้นบนแมพ

### 4.4 การเพิ่ม/ลบทีม

เฉพาะผู้ดูแลระบบ (`admin`, `super_admin`) เท่านั้นที่สามารถ:

- สร้างทีมใหม่
- ลบทีม
- แก้ไขทีม

### 4.5 การจ่ายงานอัตโนมัติ

1. กดปุ่ม `จ่ายงานอัตโนมัติ`
2. เลือกทีมที่ต้องการจ่ายงาน
3. กดยืนยัน

ระบบจะจัดสรรงานให้ทีมตามเงื่อนไขที่ตั้งไว้

### 4.6 ปิดงาน / งานไม่สำเร็จ

สำหรับงานที่ถูกจ่ายแล้วและยังไม่ปิด คุณสามารถ:

- กดปุ่ม `จบงาน` (Complete Job)
- กดปุ่ม `ไม่สำเร็จ` (Failed Job)

ถ้าเลือก `ไม่สำเร็จ` ระบบจะให้กรอกเหตุผลก่อนยืนยัน

---

## 5. ระบบคลังสินค้า

เฉพาะผู้ใช้งานที่เป็น `admin` หรือ `super_admin`

คุณจะเห็นเมนู `ระบบคลังสินค้า` เพื่อจัดการ:

- สินค้าและรุ่น
- จำนวนสต็อก
- การย้ายหรือโอนสินค้า
- ประวัติการเคลื่อนไหว

> รายละเอียดเพิ่มเติมระบบคลังสินค้าอาจขึ้นอยู่กับโมดูลที่ติดตั้งไว้ในระบบ

---

## 6. ปัญหาพบบ่อย (FAQ)

### Q1: ทำไมฉันไม่เห็นเมนู `น้ำมัน` หรือ `Dispatch`?
A: ถ้าคุณเป็น `sales` ระบบสุ่มไว้ให้ใช้เฉพาะหน้าเช็คอินเท่านั้น ส่วนเมนูอื่นจะซ่อนตามสิทธิ์

### Q2: ทำไมเมื่อกด `ส่งออก` แล้วข้อมูลซ้ำ?
A: ระบบจะกรองข้อมูลซ้ำก่อนนำออกแล้ว หากยังซ้ำ ให้ตรวจสอบว่าในฐานข้อมูลมีรายการเดียวกันหลายรอบหรือใช้งานปุ่ม `ส่งออก` หลายครั้ง

### Q3: ทำไมหน้า Dispatch แจ้ง `เชื่อมต่อล้มเหลว`?
A:
- อาจเกิด session หมดอายุ
- อาจเป็นเพราะ API คืนค่า error แทน JSON
- ให้รีเฟรชหน้าและล็อกอินใหม่

### Q4: ทำไมกด `จบงาน` แล้วไม่มีปุ่มขึ้น?
A:
- งานต้องถูกจ่ายให้ทีมก่อน
- งานที่ปิดแล้วจะไม่แสดงปุ่ม

### Q5: ทำไม `ส่งออก Excel` ไม่มีคอลัมน์ที่ต้องการ?
A: แบบฟอร์มส่งออกออกแบบมาให้ส่งเฉพาะข้อมูลสำคัญ หากต้องการเพิ่มคอลัมน์ให้ติดต่อผู้ดูแลระบบ

### Q6: ทำอย่างไรถ้าข้อมูล Excel นำเข้าไม่ได้?
A:
- ตรวจสอบว่าไฟล์เป็น `.xlsx` หรือ `.xls`
- ตรวจสอบหัวคอลัมน์ให้ตรงกับระบบ
- ตรวจสอบว่าแถวมีค่า `ลิตร` และ `ทะเบียนรถ` ด้วย

### Q7: หากระบบแจ้งว่า "กรุณาเข้าสู่ระบบใหม่"?
A: ให้ล็อกอินใหม่ เพราะ session หมดอายุหรือ token ไม่ถูกต้อง

---

## 7. เคล็ดลับสำหรับมือใหม่

- เริ่มที่ `หน้าแรก` ทุกครั้งก่อนใช้งาน
- ใช้ฟิลเตอร์วันที่ก่อนเพื่อจำกัดข้อมูล
- ถ้าจะแก้ไขข้อมูลน้ำมัน ให้ใช้ปุ่ม `เพิ่มข้อมูล` หรือ `แก้ไข` ของแถวที่ต้องการ
- หากพบปัญหา ให้บันทึกภาพหน้าจอและข้อความ error เพื่อส่งให้ผู้ดูแล

---

## 8. คำแนะนำการใช้งานขั้นสูง

### 8.1 ถ้าต้องการรายงานสรุป
- ใช้หน้า `น้ำมันและยานพาหนะ` เพื่อดูยอดรวม
- ใช้ `ส่งออก` เพื่อได้ไฟล์ Excel สำหรับตรวจสอบลึก

### 8.2 ถ้าอยากตรวจสอบงานบนแผนที่
- เปิดแท็บ `แผนที่` ในหน้า Dispatch
- กดหมุดงานเพื่อดูรายละเอียด
- ใช้ปุ่มนำทางไปยังพิกัด

### 8.3 ถ้าต้องการกลับไปหน้าเดิมหลัง logout
- ล็อกอินใหม่ที่หน้า login
- ตรวจสอบบทบาทผู้ใช้ให้ถูกต้อง

---

## 9. ติดต่อผู้ดูแล

หากพบปัญหาที่แก้เองไม่ได้ ให้ติดต่อผู้ดูแลระบบพร้อมข้อมูลต่อไปนี้:

- หน้าจอที่ใช้
- ข้อความ error
- ขั้นตอนที่ทำก่อนเกิดปัญหา
- วันที่และเวลาที่เกิดปัญหา


---

### เอกสารฉบับนี้เหมาะสำหรับ
- ผู้ใช้งานใหม่
- ช่างเทคนิค
- ผู้บริหารทีม Dispatch
- ผู้ดูแลระบบ admin/super_admin

**END**
